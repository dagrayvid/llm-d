apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: wide-ep-inference-gateway
spec:
  gatewayClassName: istio
  listeners:
    - port: 80
      protocol: HTTP
      name: default
      allowedRoutes:
        namespaces:
          from: All
